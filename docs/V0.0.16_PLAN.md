# v0.0.16 持续更新记录

状态：开发中，未上传。开始日期：2026-09-25。用户明确要求：下达 GitHub 上传命令前，后续对话决策、代码、检测和说明均归入 v0.0.16。页面公告在用户要求上传当天更新，只用 MMDD，不展示版本号。

## 基线与回滚

工作分支 `feat/v0.0.16`；`4e8b20e` 原样保存用户的 4 个未提交文件，以本地 v0.0.15 为起点，不逐项核对手动改动。回滚应创建分支或可审核的回退提交，不能清空用户后续改动。

## 已确认需求与决策

1. 参考另一分支的顶部收起图标和交互；只增加桌面侧栏收起/展开以及左侧垂直居中的半圆箭头入口，不移植其导航轨道、拖宽或版本切换功能。沉浸模式两端隐藏 MOD 悬浮球和续写框，仅桌面加宽正文并响应窗口调整字号，退出后还原。
2. 全局设置→书籍与提示词顶部增加发送预览。显示实际消息的系统/用户/助手角色、来源、顺序与原文；来源可跳转，设定原文可编辑回写。自定义楼层按当前书持久保存，允许顺序、角色、开关和内容编辑；最近发送记录只读。用户已选择“当前书持久保存＋同步源区块”。
3. 作家预设替换原文风预设命名，加强明确的写作指令；修复能复现的并发与流水线故障，提供自定义层。白鸟锁定宿主预设是特性，保持。其他无法确定的行为先记录和询问。预设独立 JSON 支持分区勾选、覆盖/新增、数字及文字/颜色来源标记，不能夹带作品；小说导出不携带本地预设。
4. 小说 JSON 完成格式识别后，选择完整/指定区块导入，展示文件内容树。用户已选择“每次选择新书或合入当前书”；合入前展示冲突策略，未勾选内容保持。
5. 优先实测长书、长文本流、高频渲染、保存开销。用已有 Node/Edge、隔离测试档案、合成作品、模拟模型接口验证；不调用真实付费模型。

## 验收门禁

- 构建、JS 语法、npm run audit、既有回归与 AI 协议；结构审计不通过不交付。
- 桌面/手机/短屏，侧栏与沉浸进出，字号和正文宽度几何，空白角色和超长无空格文本卡片宽度不低于容器 80%。
- 预览与实际发送一致；切书隔离，源编辑回写、自定义楼层持久化、白鸟锁定。
- 导入取消无写入、无效 JSON 无写入、选区导入和合入、关联 ID、一致性、预设独立导出。
- 快速重复点击、重排中的异步返回、网络错误、取消/切书后的旧请求隔离。
- 长书加载/操作/流式生成性能，结果不丢字，保存后刷新一致。

## 初始检测

基线 npm.cmd run audit 通过（154 处 v-else 配对）；npm.cmd run test:ai 23/23 通过。PowerShell 限制 npm.ps1，使用同一已安装 npm 的 npm.cmd，不修改系统策略。尚不代表新增功能已验收。
