# v0.0.16 R4：公告重写、补传上游与线上复验（2026-09-26，已上线）

## 背景：线上为什么停在 0923

上一轮交付只推送到了 `origin`（yydcm-129/Moyun），而线上 `not1a-del.github.io/Moyun/` 由 `upstream`（Not1a-del/Moyun）的 Pages 服务。`upstream/main` 停在 af9d642（v0.0.15），因此 v0.0.16 的全部功能从未上线，线上公告仍是 0923。两仓库工作区此前完全一致（033eb41），无内容丢失，只是推送目标错了。

## 本轮改动（提交 350fa1d，版本仍为 v0.0.16）

- `source/moyun.single.html`：重写 `WEB_UPDATE_ANNOUNCEMENT`。新 id `web-2026-09-26-0925-0926-update`，标题「0926更新」，正文按天分组覆盖两天全部内容——0925：侧栏收起/展开、双端沉浸模式、发送预览、作家预设独立 JSON、小说 JSON 导入、长书性能优化；0926：删除侧栏动画改即时切换、1px 残留边框修复、保存回归与章节摘要修复。不出现版本号。
- `scripts/announcement-browser-smoke.cjs`：断言从旧 0923 id 同步更新为「标题 0926更新、正文含两天内容、无版本号」，截图前缀 `announce-0926-*`。
- `index.html`、`assets/js/moyun.js`、`assets/css/moyun.css`：重新构建产物（js v=65ba0e0f824c，css v=80e1ab859cc3）。

新公告 id 与已读键（`moyun_web_update_announcement_seen`）不同值，读过 0923 公告的老用户会重新看到本次公告；线上用户从未见过 0926 公告，无冲突。

## 检测结果（全部通过）

| 范围 | 结果 |
| --- | --- |
| npm run build / audit / regress | PASS（构建、语法、既有业务断言、结构审计、AI 协议 23/23） |
| 公告冒烟（本地 4173） | 8/8 PASS：1280 与 390 双宽度，新用户先新手说明再公告、JSON 公告无版本号无横向溢出、确认后刷新不再弹、老用户直接显示新公告 |
| v0.0.16 业务专项（本地 4176） | 30/30 PASS（预览/导入/沉浸/连点/切书/长流/停止等全项） |
| 线上公告冒烟 | 8/8 PASS（同上四组断言，对 https://not1a-del.github.io/Moyun/ 实测） |
| 线上业务专项 | 30/30 PASS（同上 30 项，对线上 URL 实测） |
| 线上指纹 | 线上 HTML 引用 js v=65ba0e0f824c 与本地构建一致；线上 js 含新公告 id 与两天内容 |

测试环境：本机 Edge 153 隔离档案（9235/9236 两 headless 实例），测试后已关闭清理，不触碰日常浏览器数据。CDN 依赖 unpkg/jsdelivr/tailwind/cdnjs，本轮直连网络下正常加载；此前的节点故障已由用户关闭代理解决，代码内 CDN URL 未改动。

## 发布与回滚

- 发布提交：`350fa1d`，同时快进推送 `upstream/main`（af9d642→350fa1d，线上生效）与 `origin/main`（033eb41→350fa1d，保持两仓一致）。
- 工作分支：`fix/v0.0.16-sidebar-performance`；公告基线备份分支：`backup/v0.0.16-announce-base-033eb41`（公告重写前的 033eb41 原样）。
- 回滚：本地 `git reset --hard 033eb41` 或从备份分支重建；若需回滚线上，把 upstream/origin main 退回 af9d642（v0.0.15）需 force push，操作前必须经用户确认。

## 已知边界

- 线上 Pages 部署存在缓存窗口；若用户端仍见 0923 公告，硬刷新（Ctrl+F5）即可。
- 网络环境：git push 曾因代理节点故障挂起，本轮用户关闭代理直连后一次成功；后续如再遇挂起，先查系统代理状态而非 git 凭据。
- 公告按天分组为两段 group + howto，未逐条列出 0925 的每个细节（修复项以 R2 报告为准）。